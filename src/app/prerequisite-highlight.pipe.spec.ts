import { PrerequisiteHighlightPipe } from './prerequisite-highlight.pipe';

describe('PrerequisiteHighlightPipe', () => {
  it('create an instance', () => {
    const pipe = new PrerequisiteHighlightPipe();
    expect(pipe).toBeTruthy();
  });
});
